stages:
  - select

select-target-org:
  stage: select
  image: alpine:latest
  parallel:
    matrix:
      - TARGET_BRANCH: ["main", "qa", "uat"]
  script:
    - ./.gitlab/scripts/select_target_org.sh "$TARGET_BRANCH"
